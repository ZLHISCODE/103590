接口模式与大医二院住院包药机接口模式一致
1. 对方使用MSSqlServer数据库
2. 通过直接插入对方提供的数据表来交换数据
3. 与住院采取的同样的数据表，传输的数据格式也相同

1. 修改注册表内容，其中”药房ID“、”药房编码“必须要有值，“轮询间隔”默认是60秒，“查询天数”默认是空（当天），“剂型”默认是空（所有剂型），如果要指定剂型则按“剂型1,剂型2,剂型3”格式填写
2. 在要使用该程序的客户机上执行注册表
3. 执行脚本，其中新增过程如果是非ZLHIS用户使用要进行授权
4. 包药机的数据库连接库可以在程序中设置，设置完后要重新启动程序